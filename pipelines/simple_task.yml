---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: busybox

params:
  MY_PARAM:

run:
  path: /bin/sh
  args:
    - -c
    - |
      set -e
      echo "Param is ${MY_PARAM}"
